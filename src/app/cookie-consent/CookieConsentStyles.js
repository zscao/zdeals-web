export const styles = theme => ({
  root: {
    top: 'auto',
    bottom: 0,
    //background: 'rgba(0,0,0, 0.8)'
    opacity: 0.9,
  },
  terms: {
    fontSize: '1em',
    padding: '10px',
    '& > a': {
      color: '#fff'
    }
  }
})